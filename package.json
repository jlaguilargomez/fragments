{
  "name": "fragments",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:trial",
    "dev:trial": "VITE_TRIAL_MODE=true npm run dev -w @fragments/web",
    "dev:premium": "npm run dev -w @fragments/api & VITE_TRIAL_MODE=false VITE_ENABLE_SIGNUP=true npm run dev -w @fragments/web",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build:worker": "VITE_TRIAL_MODE=false VITE_ENABLE_SIGNUP=true npm run build -w @fragments/web && npm run build -w @fragments/worker"
  },
  "engines": {
    "node": ">=22"
  },
  "allowScripts": {
    "better-sqlite3@12.11.1": true,
    "esbuild@0.28.2": true
  }
}
